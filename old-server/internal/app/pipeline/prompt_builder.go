package pipeline

// Includes templates and prompt build funcs. If support for multiple LLMs is needed, this will move to its own domain-level interface. Specific implementations will be in infra/<llm_idendifier>
