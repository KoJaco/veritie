## ✅ **Today's Build Plan – TODO**

### 1️⃣ Frontend Styling Refactor

-   [ ] Review existing components and extract common spacing / typography
-   [ ] Create/update `theme.ts` (or equivalent) with:

    -   [ ] Font sizes / weights
    -   [ ] Base spacing/padding scale
    -   [ ] Colour palette

-   [ ] Apply global styles / primitives
-   [ ] Refactor:

    -   [ ] Buttons
    -   [ ] Inputs / Textareas
    -   [ ] Cards / Panels
    -   [ ] Modal scaffolding

-   [ ] Quick smoke test across:

    -   [ ] Dashboard
    -   [ ] Example/Playground pages

---

### 2️⃣ PHI Redaction Implementation

#### 🔽 Model Download + Export

-   [ ] Download `emilyalsentzer/Bio_ClinicalBERT` locally
-   [ ] Export to ONNX (`optimum-cli export onnx --task token-classification`)
-   [ ] Commit model → `models/phi_clinicalbert/onnx/`

#### 🧩 Infra Layer

-   [ ] Create `infra/phi_redactor` package
-   [ ] Implement `New()` → loads ONNX model
-   [ ] Implement `Redact()` → returns `(masked, mapping)`

#### 🧠 App Layer

-   [ ] Add `phiRedactor` dependency to `RedactionService`
-   [ ] Update redaction sequence to `PII → PHI`

#### 🔌 Transport Layer

-   [ ] Apply `RedactionService.Redact()` before sending to LLM
-   [ ] Store mapping in stream/session object
-   [ ] Substitute mapping back after LLM response

#### ✅ Test

-   [ ] Manual integration test (frontend voice → backend → function call)
-   [ ] Confirm `[PATIENT]` + `[DATE]` masking
-   [ ] Confirm context accuracy is preserved (summarisation still works)

---

### 3️⃣ Config-Ping Endpoint (Planning Only)

-   [ ] Create empty handler in `transport/http/config_ping.go`
-   [ ] Define `ConfigPingRequest` & `ConfigPingResponse` structs

    -   `ConfigPingResponse` should include:

        -   `approxCostPerMinute`
        -   `functionSchemaQualityAnalysis` _(placeholder)_
        -   `structuredOutputSchemaQualityAnalysis` _(placeholder)_

-   [ ] Add TODO comments for:

    -   Estimated cost algorithm
    -   LLM-based schema analysis integration

-   [ ] Register the route in router (_do not implement logic yet_)
