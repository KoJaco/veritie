# What this directory is for

Archived documentation that:

- Does not fit into the structure (`adr`, `architecture`, `contracts`, `clarifications`, `decisions`, `fixtures`, `planning`).
- Was partially complete and discarded
- Was acting as a temp 'todo' and is now complete
